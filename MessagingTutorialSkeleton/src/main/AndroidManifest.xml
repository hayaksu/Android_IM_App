<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.messagingtutorialskeleton" >

    <!-- Permissions needed by parse and snitch -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Light.NoTitleBar"
        android:name="com.sinch.messagingtutorialskeleton.MyApplication"
        android:debuggable="true">
        <activity
            android:name="com.sinch.messagingtutorialskeleton.LoginActivity"
            android:screenOrientation="portrait" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.sinch.messagingtutorialskeleton.ListUsersActivity"
            android:label="@string/title_activity_list_users" >
        </activity>

        <service

            android:name="com.sinch.messagingtutorialskeleton.MessageService"
            android:enabled="true" />

        <activity
            android:name="com.sinch.messagingtutorialskeleton.MessagingActivity"
            android:label="@string/title_activity_messaging" >
        </activity>
    </application>

</manifest>
